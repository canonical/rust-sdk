name: Renovate check

on:
  pull_request:
    paths:
      - renovate.json

jobs:
  renovate-check:
    permissions:
      contents: read
      pull-requests: read
    uses: canonical/sdkcraft-actions/.github/workflows/renovate-check.yml@main
